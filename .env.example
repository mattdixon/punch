# Database
DATABASE_URL="postgresql://punch:punch@localhost:5434/punch"

# NextAuth
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Email (optional — falls back to temp passwords if not set)
# RESEND_API_KEY="re_xxxxx"
# EMAIL_FROM="Punch <no-reply@yourdomain.com>"
# EMAIL_OVERRIDE_TO="you@example.com"  # Dev only: redirect all emails to this address

# Stripe (optional — billing features hidden if not set)
# Plans/prices are auto-created in Stripe on first checkout.
# STRIPE_SECRET_KEY="sk_test_xxxxx"
# STRIPE_WEBHOOK_SECRET="whsec_xxxxx"
