services:
  db:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: punch
      POSTGRES_PASSWORD: punch
      POSTGRES_DB: punch
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
